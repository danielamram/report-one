import { Pipe, PipeTransform } from '@angular/core';

/**
 * Generated class for the ParseFollowingPipe pipe.
 *
 * See https://angular.io/docs/ts/latest/guide/pipes.html for more info on
 * Angular Pipes.
 */
@Pipe({
  name: 'parseFollowing',
})
export class ParseFollowingPipe implements PipeTransform {
  /**
   * Takes a value and makes it lowercase.
   */
  transform(followings: any) {
    return ;
  }
}
